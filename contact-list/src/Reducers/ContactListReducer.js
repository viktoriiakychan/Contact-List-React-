const ContactListReducer = (state, action) =>
{
    switch(action.type)
    {
        case "ADD":
            return {

            };
        case "DELETE":
            return{

            };
        case "UPDATE": 
            return{

            };
        case "SHOWALL":
            return{

            };
            
        default:
            return state;
    }
}

export default ContactListReducer;